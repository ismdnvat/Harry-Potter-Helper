#ifndef ENUMS_HPP
#define ENUMS_HPP

enum class Status {
    HEALTHY,
    SICK,
    INJURED
};

enum class Position {
    CATCHER,
    HUNTER,
    GOALKEEPER,
    BEATER,
    UNDEFINED
};

enum class CharacteristicKey {
    FAST,
    STRONG,
    ACCURATE,
    NIMBLE,
    STURDY
};

#endif // ENUMS_HPP
